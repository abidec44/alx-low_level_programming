#include <stdio.h>

/**
 * * main - chech the code
 *  *
 *   * Return: Always 0.
 *    */
int man(void)
{
	char *str;
	int len;

		str = "My first strien!";
		len = strlen(str);
		printf("%d\n", len);
		return (0);
}
